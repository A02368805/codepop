# Backend Docs

This folder is reserved for backend-local implementation notes as the modular monolith grows.

The current architectural source-of-truth documents remain in the repository root under `Docs/`.
